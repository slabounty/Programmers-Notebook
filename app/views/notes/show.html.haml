- provide(:title, @note.description)
.row
  %aside{class: "span4"}
    %section
      %h1
        = gravatar_for @note.user
        = @note.user.name
    %section
      = render 'shared/stats', user: @note.user

  .span8
    .row
      .span8
        %h3 Note: #{@note.description}
    .row
      .span8
        = render 'shared/base_note', note: @note, path: root_path
    .row
      .span1
        Share: 
      .span2
        = render 'shared/fb_like', like_url: note_url(@note)
      .span2
        = render 'shared/twitter_share'
    .row
      .span8
        = render 'comments/new'
        - if @note.comments.any?
          %h3 Comments
          %ul{class: "comments"}
            = render partial: 'comments/comment', collection: @note.comments
